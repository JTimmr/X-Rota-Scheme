# TODO

- Build option to schedule according to the usual pipeline, but without posting on X, without posting in the Discord channels, or without both of them. So we can experiment with different setups while still looking at what's posted from one place, being the Discord channel. Need to have these options while scheduling, and also buttons on scheduled posts that act as a toggle between these options.

- Build the option to schedule a post as usual, but have a delay in when it gets posted in the Discord channels. Everything else works the same. Should have a dropdown menu while scheduling, and it should also be possible somehow to change it on posts that have already been scheduled.

- The cashtag doesn't work very well while scheduling (`$FARTBOY`). Gotta check whether we can sort that.

- Check whether it's possible to schedule a poll on X through our scheduling bot.
